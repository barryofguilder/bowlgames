import { longTime } from '../../../helpers/long-time';
import { module, test } from 'qunit';

module('Unit | Helper | long time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = longTime(42);
  assert.ok(result);
});
