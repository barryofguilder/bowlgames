import { shortTime } from '../../../helpers/short-time';
import { module, test } from 'qunit';

module('Unit | Helper | short time');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = shortTime(42);
  assert.ok(result);
});
